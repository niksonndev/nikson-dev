# Google Tag Manager container ID (optional). Format: GTM-XXXXXXX
# Only used when set; script is not injected if missing or invalid.
VITE_GTM_ID=

# Canonical URL for meta tags (Open Graph, etc.). Used in production.
# Example: https://www.nikson.dev
VITE_SITE_URL=
